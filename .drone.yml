kind: pipeline
name: zeek

platform:
  arch: arm64
  
steps:
- name: submodules
  image: ubuntu/git
  commands:
  - git submodule update --recursive --init

- name: build
  image: gcc
  commands:
  - uname -a
  - git submodule update --recursive --init
  - ./configure
  - make
